# 球员能力值金字塔分布 - 验证清单

- [x] 检查点 1: `CONFIG.LEAGUE_ABILITY_RANGES` 已在 `core.js` 正确定义，包含 6 个级别的 `min` 和 `max`
- [x] 检查点 2: 6 个能力范围合并后正好覆盖 [1, 200]，无空隙无重叠
- [x] 检查点 3: `generatePlayer(1)` 生成的球员能力均在 [167, 200]
- [x] 检查点 4: `generatePlayer(6)` 生成的球员能力均在 [1, 34]
- [x] 检查点 5: 中间级别（L2-L5）的能力范围分别为 [134,166], [101,133], [68,100], [35,67]
- [x] 检查点 6: 大量样本下各联赛能力平均值接近其中点（±3）
- [x] 检查点 7: 完整生成 3000 球员后，L1 平均能力 > L2 > ... > L6，L1-L6 差 ≥ 100
- [x] 检查点 8: 球员潜力 `generatePotential()` 中 `Math.min(200, ...)` 仍然限制在 200 以内
- [x] 检查点 9: 球队平均实力在 L1 远高于 L6（可在游戏界面"实力"显示中验证）
- [x] 检查点 10: 代码可读性 - 配置表在 CONFIG 中集中定义，数据结构一眼可读